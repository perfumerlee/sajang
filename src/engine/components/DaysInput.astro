---
import NumericInput from './NumericInput.astro';
const { id, label, ...props } = Astro.props;
---
<NumericInput id={id} label={label} {...props} kind="days" unit={props.unit ?? '일'} min={props.min ?? 1} />
