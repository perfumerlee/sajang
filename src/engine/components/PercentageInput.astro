---
import NumericInput from './NumericInput.astro';
const { id, label, ...props } = Astro.props;
---
<NumericInput id={id} label={label} {...props} kind="percentage" unit={props.unit ?? '%'} min={props.min ?? 0} max={props.max ?? 100} />
